# 阶段 2 验收记录

日期：2026-09-13  
验收角色：UX 设计师 + 测试

## 当前结论

**OCR 语言包问题已修复；完整阶段 2 端到端验收仍需在浏览器中继续。**

代码静态检查、自动测试、生产构建和面板收起/恢复视觉流程通过。此前阻塞三张图片测试的 OCR 语言包问题已修复：中文和英文 `traineddata` 已随项目发布，Worker 改为从 `/tessdata` 加载，不再依赖运行时 CDN。

## 已验证项目

- 段落识别规则测试 4/4 通过。
- `git diff --check` 通过。
- 生产构建通过。
- 识别 Worker 复用、同图缓存和重新识别失败保留旧结果已落地。
- Kimi 瞬时错误重试与 60 秒请求超时已落地。
- 空状态下设置面板收起后，画布扩展且右上角展示 Figma 展开图标。
- 设置面板可恢复，表单区域仍可见。
- Impeccable 界面检测无发现项。

## 三张测试图执行记录

本次测试文件：

- `/Users/huangyuting/Desktop/测试 1.jpg`：Clash Verge 深色设置页；预期识别设置分区中的说明、提示和功能限制，不应把导航、开关、字段标签合并为业务段落。
- `/Users/huangyuting/Desktop/测试 3.jpg`：带蒙层的设置模态框；预期只识别前景模态框内部的账户安全说明，忽略模态框外背景。
- `/Users/huangyuting/Desktop/测试2.jpg`：百度账号设置页；预期拆分绑定手机说明、按钮/导航、右侧帮助卡片，避免整页合并。

首次执行结果：

1. 三张图均已纳入测试清单并尝试通过本地分析接口执行。
2. 原 OCR 阶段因 Tesseract Worker 无法从 CDN 加载 `chi_sim+eng` 语言包而失败（`fetch failed`）。
3. 已将语言包下载到 `public/tessdata/`，并验证开发服务器返回 `200`；Node OCR 使用本地语言包成功识别《测试 3.jpg》，输出包含“常规”“保护你的账户”“多重身份验证”等文字。
4. 因首次执行在修复前中断，本轮仍不把三张图的候选数量、段落边界、模态框过滤和优化准确率记为最终通过；修复后应重新执行三张图端到端测试。

## 待在可用 OCR 环境复核

使用以下文件上传测试：

- `/Users/huangyuting/Desktop/测试 1.jpg`
- `/Users/huangyuting/Desktop/测试 3.jpg`
- `/Users/huangyuting/Desktop/测试2.jpg`

重点复核：

1. 首次识别与同图再次识别耗时，确认第二次跳过 OCR；
2. 模态框焦点范围、段落合并/拆分和候选框位置；
3. 右键复制、复制成功提示和剪贴板内容；
4. 四向手柄、删除、新增框选与局部 OCR；
5. 单条重新优化是否真正改变文案，且不影响其他结果；
6. 取消识别、连续重新识别和切换图片后的结果隔离。

复核前置条件：重新启动开发服务器并使用系统浏览器完成本地文件选择；完成后记录三张图的实际 OCR 块、候选框截图和耗时。

## 问题分级

当前未发现已确认的 P0 阻塞问题。端到端图片链路未完成前，不将阶段 2 标记为最终通过。
